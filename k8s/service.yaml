apiVersion: v1
kind: Service
metadata:
  name: employee-salary-app
spec:
  type: NodePort
  selector:
    app: employee-salary-app
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30011
